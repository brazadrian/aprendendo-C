#include <stdio.h>

int main()
{
    // declarando variáveis
    int A, B, X;

    // entrada
    scanf("%d", &A);
    scanf("%d", &B);

    // processamento
    X = A + B;

    // saida
    printf("X = %d\n", X);

    return 0;
}
