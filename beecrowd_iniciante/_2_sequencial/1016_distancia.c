#include <stdio.h>

int main()
{
    // declaração de variáveis
    int distancia, tempo;

    // entrada de dado
    scanf("%d", &distancia);

    // processamento
    tempo = distancia * 2;

    // saída
    printf("%d minutos\n", tempo);

    return 0;
}
