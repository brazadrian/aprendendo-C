#include <stdio.h>

int main()
{

    // declarando variáveis
    int n1, n2, prod;

    // entrada de dados
    scanf("%d", &n1);
    scanf("%d", &n2);

    // processamento
    prod = n1 * n2;

    // saída
    printf("PROD = %d\n", prod);

    return 0;
}
