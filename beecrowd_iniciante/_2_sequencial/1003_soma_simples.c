#include <stdio.h>

int main()
{

    // declarando variáveis 
    int a, b, soma;

    // entrada de dados
    scanf("%d", &a);
    scanf("%d", &b);

    // processamento
    soma = a + b;

    // saída
    printf("SOMA = %d\n", soma);

    return 0;
}
