#include <stdio.h>

/* 
void imprime_vetor(int *V, int tamV)
{
    for (int i = 0; i < tamV; i++)
    {
        printf("%d\n", V[i]);
    }
}
 */

/*
void ordena_vetor(int *v, int tamV){
    int ram = 0;
    for(int i = 0; i < tamV; i++){
        for(int j = 0; j < tamV; j++){
            if(v[i] > v[j]){
                ram = v[i];
                v[i] = v[j];
                v[j] = ram;
            }
        }
        printf("%d", v[i]);
    }
}
 */